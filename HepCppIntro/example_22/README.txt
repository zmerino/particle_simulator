Before building this example a recent version of HepMC must be 
installed.  A recent version can be installed by

cd ../support/install-scripts/
./build-HepMC.sh

Then the executable can be built by typing:

cd src/; make

The program can be run by typing

./Pi0Decay.exe
